{
  "name": "gw-member-map",
  "version": "1.0.0",
  "description": "A spatial, opt-in member directory on a world map, hosted on Cloudflare Workers + D1.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "vendor": "node scripts/fetch-vendor.mjs"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_PASSWORD": {
        "description": "Password for the `/admin` dashboard sign-in. Choose a strong, unique value."
      },
      "SESSION_SECRET": {
        "description": "Secret used to sign admin session cookies. Generate one with `openssl rand -base64 32`."
      }
    }
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "leaflet": "1.9.4",
    "leaflet.markercluster": "1.5.3",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
